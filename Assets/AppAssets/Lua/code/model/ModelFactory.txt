local CSGameObject = CS.UnityEngine.GameObject
local CSWidgetNode = CS.Framework.Code.Widget.WidgetNode
ModelFactory={
	
}
local FORMAT_MODEL_NAME = "model.%s"

function ModelFactory:CreateModel( modelName )
	local modelTmp = require (string.format(FORMAT_MODEL_NAME, modelName))
	return modelTmp:Create()
end
