BaseView = {
	m_EventHandle = {},
	m_View,
}


function BaseView:Create(View)
	local obj = {}
	setmetatable( obj, { __index = self })
	obj.m_View = View
	for k,v in pairs(obj.m_View._widgets) do
		obj[k] = v
	end

	obj:init()
	return obj
end

--虚方法，子类重写
function BaseView:init()
end

