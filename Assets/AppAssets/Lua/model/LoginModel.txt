local LoginModel = {
	m_Id = 1,
	m_Name = "LoginModel",
}

setmetatable( LoginModel, { __index = BaseModel})

local PanelDefine = {
	login = "LoginView"
}

function LoginModel:OnStart()
	print("LoginModel:Start")
    local loginPresender = ViewFactory:CreateView(self, PanelDefine.login)
end

function LoginModel:OnRelease()
	print("LoginModel:Release")
end

return LoginModel