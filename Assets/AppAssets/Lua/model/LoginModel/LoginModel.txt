local LoginModel = {
	m_Id = 1,
	m_Name = "LoginModel",
}

setmetatable( LoginModel, { __index = BaseModel})

local PanelDefine = {
	login = "LoginView",
	loading = "LoadingView",
}

function LoginModel:OnStart()
	print("LoginModel:Start")


	local loginPresender = ViewFactory:CreateView(PanelDefine.login, LAYER_NAME.TOP)
	loginPresender:SetLabel(C.CityStarConfig[1].count)

	local loadingPresender = ViewFactory:CreateView(PanelDefine.loading, LAYER_NAME.TOP)


--	ViewFactory:CreateViewAsync(PanelDefine.login,function ( presender )
--        presender:SetLabel(C.CityStarConfig[1].npcId)
--    end)
    
end

function LoginModel:EventTest1( param )
	print("OnNotify_Test_1 - " .. param)
end

function LoginModel:EventTest2( param )
	print("OnNotify_Test_2 - " .. param)
end

function LoginModel:EventTest3( param )
	print("OnNotify_Test_3 - " .. param)
end

function LoginModel:OnRelease()
	print("LoginModel:Release")
end

return LoginModel