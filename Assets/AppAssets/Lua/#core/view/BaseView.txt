BaseView = {
	m_EventHandle = {},
	m_csView,
}


function BaseView:Create(View)
	local obj = {}
	setmetatable( obj, { __index = self })
	obj.m_csView = View
	for k,v in pairs(obj.m_csView._widgets) do
		obj[k] = v
	end

	obj:init()
	return obj
end

--虚方法，子类重写
function BaseView:init()
end

