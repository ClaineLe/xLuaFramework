BaseView = {
	m_EventHandle = {},
}


function BaseView:Create(csView)
	local obj = {}
	setmetatable( obj, { __index = self })
	for k,v in pairs(csView._widgets) do
		obj[k] = v
	end
	obj:init()
	return obj
end

--虚方法，子类重写
function BaseView:init()
end

