BasePresender = {
	m_csView,
}
function BasePresender:Create(view)
	local obj = {}
	setmetatable( obj, { __index = self })
	obj.m_csView = view
	obj:init()
	return obj
end

--虚方法，子类重写
function BasePresender:init( )
end

